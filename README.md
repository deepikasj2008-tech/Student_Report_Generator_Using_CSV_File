# Student_Report_Generator_Using_CSV_File

The Student Report Generator is a Python program that manages student academic records and generates report cards using a CSV file. It allows users to store student details, view records, and calculate performance based on marks.

The data is saved in a file named students.csv, ensuring that all records are stored permanently.
